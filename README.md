# Japan Births Predictor

<<<<<<< HEAD
A Flask web app serving a Random Forest regression model trained on Japan
birth statistics (1899-2023) to predict total births for a given year.
=======
A simple Flask web app that serves a linear regression model trained on the
[Japan Birth Statistics 1899–2023](https://www.kaggle.com/datasets/hideos/japan-birth-statistics-18992023)
dataset. Given a year, it predicts the number of births.
>>>>>>> dafb54910514679376665084a32a07afd0de0978

## Project structure

```
.
<<<<<<< HEAD
├── app.py
├── train.py
├── japan_birth_statistics_1899_2023.csv
├── model/
│   └── birth_model.joblib
├── requirements.txt
├── render.yaml
=======
├── app.py              # Flask app (serves predictions)
├── train.py             # Downloads dataset via kagglehub and trains the model
├── model/
│   └── birth_model.joblib   # Trained model (generated by train.py)
├── requirements.txt
├── render.yaml           # Render.com deploy config
>>>>>>> dafb54910514679376665084a32a07afd0de0978
├── LICENSE
└── README.md
```

## Setup (local)

<<<<<<< HEAD
```bash
pip install -r requirements.txt
python train.py      # regenerates model/birth_model.joblib from the CSV
python app.py          # runs locally at http://localhost:5000
```
=======
1. Clone the repo and install dependencies:
   ```bash
   pip install -r requirements.txt
   ```

2. Set up Kaggle API credentials so `kagglehub` can download the dataset
   (place `kaggle.json` in `~/.kaggle/`, or run `kagglehub.login()`).
   See [Kaggle API docs](https://github.com/Kaggle/kaggle-api#api-credentials).

3. Train the model:
   ```bash
   python train.py
   ```
   This downloads the dataset, trains a linear regression model, and saves
   it to `model/birth_model.joblib`. Check the printed column names — if
   `train.py` picks the wrong columns, adjust the `year_col` / `birth_col`
   detection near the top of the script.

4. Run the app locally:
   ```bash
   python app.py
   ```
   Visit `http://localhost:5000`.
>>>>>>> dafb54910514679376665084a32a07afd0de0978

## API usage

```bash
curl "http://localhost:5000/predict?year=2025"
```

<<<<<<< HEAD
## Deploying to Render.com

1. Push this repo to GitHub, **including `model/birth_model.joblib`** —
   Render's build does not run `train.py`.
2. On Render.com, create a **Web Service** (or use Blueprint with `render.yaml`)
   connected to this repo.
3. Build Command: `pip install -r requirements.txt`
   Start Command: `gunicorn app:app`
4. If Render ignores `render.yaml`'s Python version, set
   `PYTHON_VERSION=3.11.0` manually under the service's Environment settings.

## Model

Random Forest Regressor (300 trees) on year -> total_births.
On a held-out 20% test split: **R^2 ~ 0.956, MAE ~ 49,000 births**.
This captures the non-linear rise-then-decline pattern in Japan's births
much better than plain linear or low-degree polynomial regression (which
topped out around R^2 ~ 0.78-0.84 on the same split).

Caveat: Random Forest predictions for years far outside the training range
(e.g. 2050+) will just flatten out near the nearest training years' values,
since trees cannot extrapolate trends. It's most reliable for years within
or close to 1899-2023.
=======
Response:
```json
{"year": 2025, "predicted_births": 812345}
```

## Deploying to Render.com

1. Push this repo to GitHub, **including the trained `model/birth_model.joblib`
   file** (Render's build step does not run `train.py`, so the model must
   already be committed).
2. On [Render.com](https://render.com), create a new **Web Service** and
   connect this repository.
3. Render will pick up `render.yaml` automatically, or set manually:
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `gunicorn app:app`
4. Deploy. Render will give you a live URL such as
   `https://japan-births-predictor.onrender.com`.
>>>>>>> dafb54910514679376665084a32a07afd0de0978

## License

MIT — see [LICENSE](LICENSE).
